let gql;_f75‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers]]);_f75‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql `
    interface List {
        items: [Node!]!
        totalItems: Int!
    }
`
       const resolvers = {
    List: {
        __resolveType: () => null,
    }
}